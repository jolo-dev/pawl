{
  "name": "gridx-ev",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "bootstrap:local": "npm run localstack && npm run cdklocal bootstrap --all",
    "cdklocal": "AWS_ACCESS_KEY_ID=test AWS_SECRET_ACCESS_KEY=test AWS_DEFAULT_REGION=eu-central-1 cdklocal --app 'npx tsx local.dev.ts'",
    "deploy": "cdk deploy --all",
    "deploy:local": "npm run localstack && npm run bootstrap:local && npm run cdklocal deploy -- --all",
    "dev": "npm run localstack && npm run cdklocal watch",
    "localstack": "docker compose -f ./node_modules/@pawl/cdk/docker-compose.yml up -d",
    "remove": "cdk destroy --all",
    "remove:local": "npm run localstack && npm run cdklocal destroy -- --all",
    "synth:local": "npm run cdklocal synth",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@pawl/cdk": "../../packages/cdk",
    "@pawl/lambda": "../../packages/lambda",
    "aws-cdk-local": "^3.0.1",
    "cdk-booster": "^1.0.1"
  },
  "keywords": [],
  "author": "",
  "description": "From the docs tutorial"
}
